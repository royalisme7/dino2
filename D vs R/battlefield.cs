using System;
using System.Collections.Generic;
using System.Text;

namespace D_vs_R
{
    public class battlefield
        public List<weapon> weaponchoice = new List<new weapon>()
    {
        //where the fleet of robots and herd of dinos battle
        // member variables
        public fleet fleetOfRobots = new fleet();
        public herd herdtFleet = new herd();
        // add the hehrd

        dinoherd.Add(newdino1)
         dinoherd.Add(newdino2)
        dinoherd.Add(newdino3)

       // add the fleet

       robotFleet.Add(newrobot1);
        robotFleet.Add(newrobot2);
        robotFleet.Add(newrobot3);

        // constructor
        public battlefield()
        {
            fleetOfRobots.PopulateFleet();
        }

        public void DoBattle();
        {
            fleetOfRobots.robotFleet[0].attackPower();
            herdtFleet.Populateheard[0].attackPower();



        }
        
           
        
        
        
    }
}
